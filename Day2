import math
"""
将华氏温度转换为摄氏温度
F = 1.8C + 32

"""


f = float(input('请输入华氏温度: '))
c = (f - 32) / 1.8
print('%.1f华氏度 = %.1f摄氏度' % (f, c))



'''

输入半径计算圆的周长和面积

'''
r = float(input('请输入圆的半径：'))
l = 2*math.pi*r
s = math.pi*r*r
print('这个圆的周长为：%.2f,面积为%.2f。'%(l,s))



'''
输入年份判断是不是闰年。
'''
year = int(input('请输入年份: '))
# 如果代码太长写成一行不便于阅读 可以使用\或()折行
is_leap = (year % 4 == 0 and year % 100 != 0 or
           year % 400 == 0)
print(is_leap)
